random(a,b)
  return math(math(0, 'random')*(b - a + 1) + a, 'floor')


makeMeFly(duration=5s, delay=0s, negative=-50px, positive=50px)
    animation-duration: duration;
    animation-timing-function: ease-in-out;
    animation-delay: delay;
    animation-iteration-count: infinite;
    animation-direction: alternate;
    animation-fill-mode: none;
    animation-play-state: running;
    $rand = myMove + random(10,1000000);
    animation-name: $rand;
    @keyframes {$rand}
        0% 
            transform: translateY(negative);
            // transform: translateY(- + negative);
            -webkit-transform: translateY(negative);
            // -webkit-transform: translateY(- + negative);
            -moz-transform: translateY(negative);
            // -moz-transform: translateY(- + negative);
            -o-transform: translateY(negative);
            // -o-transform: translateY(- + negative);
        100% 
            transform: translateY(positive);
            -webkit-transform: translateY(positive);
            -moz-transform: translateY(positive);
            -o-transform: translateY(positive);
        
    

.landing-page-background:before,
.landing-page-background:after
    makeMeFly(12s, 0s, -90px, 90px)

// .landing-page-background:after
//     makeMeFly(18s, 0s)

.landing-page-background video
    makeMeFly(10s, 3s)

.landing-page-background img.logo
    makeMeFly(6s, 0s, -20px)

.landing-page-background.hero  h1
    makeMeFly(6s, 0s, -20px, 15px)

.landing-page-background .random-svgs-container
    makeMeFly(8s, 0s, -40px, 40px)

.landing-page-background .hero-features-component h1,
.landing-page-background .hero-features-component .features-container,
.landing-page-background.downloads-container h1,
.landing-page-background.downloads-container h3,
.landing-page-background .download-page-component .container
    makeMeFly(8s, 0s, -20px, 20px)


@media only screen and (max-width:600px) 
    .landing-page-background video
        makeMeFly(10s, 3s, -20px, 30px)

    .landing-page-background img.logo
        makeMeFly(6s, 0s, 0px, 55px)


makeMeRotate(duration=5s, delay=0s, deg1=0deg, deg2=359deg)
    $rand = myMove + random(10,1000000);
    animation-name: $rand;
    animation: duration linear delay infinite normal none running $rand;
    animation-timing-function: ease-in-out;

    @keyframes {$rand} 
        from {
            transform: rotate(deg1);
        }
        to {
            transform: rotate(deg2);
        }

.landing-page-background .random-svg.hexa
    makeMeRotate(12s, 0s, 0deg, 360deg)

.landing-page-background .random-svg.triangle
    makeMeRotate(10s, 0s, 0deg, 360deg)