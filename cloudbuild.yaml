steps:
# install
- name: 'gcr.io/cloud-builders/npm'
  args : ['install']

# build
- name: 'gcr.io/cloud-builders/npm'
  args : ['run','cloud-build']

# deploy
- name: 'gcr.io/$PROJECT_ID/firebase'
  args : ['deploy']